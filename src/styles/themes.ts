// Tema principal VExpenses

export const lightTheme = {
  colors: {
    // Cores principais
    primary: "#0082f5", // Azul médio (botões em fundo claro, shapes)
    secondary: "#4ac0ff", // Azul claro (palavras em destaque em fundo azul escuro)
    accent: "#3fa1ff", // Azul logo (símbolo do VExpenses)

    // Cores de fundo
    background: "#f9f9f9", // Cinza claro 2 (fundo claro)
    surface: "#efefef", // Cinza claro 1 (grafismos de fundo)

    // Cores de texto
    textPrimary: "#003b75", // Azul escuro (texto em negrito, elementos decorativos)
    textSecondary: "#666666", // Cinza (texto corrido em fundo branco)
    textOnPrimary: "#ffffff", // Branco (texto em botões, títulos em fundo azul escuro)

    // Cores de estado (sucesso, erro, aviso, info)
    success: "#28a745", // Verde (sucesso)
    error: "#dc3545", // Vermelho (erro)
    warning: "#ffc107", // Amarelo (aviso)
    info: "#17a2b8", // Azul info (informações)
  },
};

export const darkTheme = {
  colors: {
    // Cores principais
    primary: "#0A8DFF", // Azul médio (botões em fundo claro, shapes)
    secondary: "#0074B3", // Azul claro (palavras em destaque em fundo azul escuro)
    accent: "#0064C2", // Azul logo (símbolo do VExpenses)

    // Cores de fundo
    background: "#050505", // Cinza claro 2 (fundo escuro)
    surface: "#0F0F0F", // Cinza claro 1 (grafismos de fundo)

    // Cores de texto
    textPrimary: "#8AC4FF", // Azul claro (texto em negrito, elementos decorativos)
    textSecondary: "#999999", // Cinza (texto corrido em fundo escuro)
    textOnPrimary: "#000000", // Preto (texto em botões, títulos em fundo azul escuro)

    // Cores de estado (sucesso, erro, aviso, info)
    success: "#218838", // Verde (sucesso)
    error: "#c82333", // Vermelho (erro)
    warning: "#e0a800", // Amarelo (aviso)
    info: "#138496", // Azul info (informações)
  },
};

// Tema(s) extra(s)

export const lightTheme2 = {
  colors: {
    // Cores principais
    primary: "#069c3d",
    secondary: "#45c469",
    accent: "#34ba67",

    // Cores de fundo
    background: "#e2d6dc",
    surface: "#d9c8d1",

    // Cores de texto
    textPrimary: "#00481d",
    textSecondary: "#653d4c",
    textOnPrimary: "#e0e0e0",

    // Cores de estado (sucesso, erro, aviso, info)
    success: "#28a745",
    error: "#dc3545",
    warning: "#ffc107",
    info: "#17a2b8",
  },
};

export const darkTheme2 = {
  colors: {
    // Cores principais
    primary: "#62F999",
    secondary: "#3BBA5F",
    accent: "#44CA77",

    // Cores de fundo
    background: "#2A1E24",
    surface: "#36262E",

    // Cores de texto
    textPrimary: "#B8FFD4",
    textSecondary: "#C299A8",
    textOnPrimary: "#1F1F1F",

    // Cores de estado (sucesso, erro, aviso, info)
    success: "#28a745",
    error: "#dc3545",
    warning: "#ffc107",
    info: "#17a2b8",
  },
};
